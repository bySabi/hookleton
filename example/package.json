{
  "name": "hookleton-example",
  "version": "0.0.1",
  "description": "Hookleton example",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export:gh-pages": "next build && next export && node misc/copyStatic.js && node misc/copyAll.js",
    "export:local": "NODE_ENV=development next build && NODE_ENV=development next export && node misc/copyStatic.js",
    "cleanup": "node misc/removeStatic.js",
    "deploy": "npm run cleanup && npm run export:gh-pages"
  },
  "keywords": [
    "Example",
    "Hookleton"
  ],
  "author": "Félix A.A. <bysabi.files@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "constate": "^1.0.0",
    "hookleton": "^0.4.6",
    "next": "^8.0.4",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "victory": "^32.0.2"
  },
  "devDependencies": {
    "fs-extra": "^7.0.1"
  }
}
